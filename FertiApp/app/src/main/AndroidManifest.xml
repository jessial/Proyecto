<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jessica.fertiapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RolActivity" />
        <activity android:name=".ui.saveUser.component.SaveUserView" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MenuCultivoActivity" />
        <activity android:name=".MenuAnalisisActivity" />
        <activity android:name=".CrearCultivoActivity" />
        <activity android:name=".IngresarParcelaActivity" />
        <activity android:name=".FincaActivity" />
        <activity
            android:name=".IngresarAnalisisActivity"
            android:label="@string/title_activity_ingresar_analisis"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NavegacionPrincipalActivity"
            android:label="@string/title_activity_navegacion_principal"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>